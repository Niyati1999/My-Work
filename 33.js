function loop(){
    let products = ['Detol','Cinthol','Hamam','Dove'];
   // products.forEach((data,i)=>{
     //   console.log(data,i);
    //})
    products.forEach(fun1);
}
function fun1(value,position){
    console.log(value,value.length,position);
}